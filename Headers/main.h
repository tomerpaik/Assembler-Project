#ifndef MAIN_H
#define MAIN_H

#include "pre_assembler.h"
#include "first_pass.h"
#include "second_pass.h"
#include "general_functions.h"

/**
 * @brief Main function that processes the input files through the assembler stages.
 *
 * This function is the main entry point of the assembler program. It processes each input
 * file through the preprocessor, first pass, and second pass stages, handling errors and
 * cleaning up resources as needed.
 *
 * @param argc The number of command-line arguments.
 * @param argv An array of strings containing the command-line arguments.
 * @return Returns 0 on successful completion, or exits on failure.
 */

#endif /* MAIN_H */